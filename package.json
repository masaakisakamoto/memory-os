{
  "name": "memory-os",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:api": "pnpm --filter @memory-os/api dev",
    "dev:worker": "pnpm --filter @memory-os/worker dev",
    "dev:mcp": "pnpm --filter @memory-os/mcp-server dev",
    "db:migrate": "node infra/db/migrations/run.js",
    "db:seed": "node infra/db/seeds/run.js",
    "continuity-check": "tsx cli/continuity-check.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
